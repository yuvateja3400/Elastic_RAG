# === Elasticsearch (used later, placeholder now) ===
ELASTIC_URL=http://localhost:9200
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=changeme
ELASTIC_INDEX_NAME=rag_documents_v1

# === Retrieval defaults (used later) ===
DENSE_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
ELSER_MODEL_ID=.elser_model_2
TOP_K_DEFAULT=5
RRF_K=60

# === Google Drive (FR1) ===
GDRIVE_FOLDER_ID=
GDRIVE_SERVICE_ACCOUNT_JSON_PATH=./secrets/service_account.json

# === Hugging Face (FR4 – hosted LLM; placeholder now) ===
HF_API_TOKEN=
HF_LLM_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
HF_TIMEOUT_SECONDS=30

# === Chunking (FR1) ===
CHUNK_SIZE_TOKENS=300
CHUNK_OVERLAP_TOKENS=60

# === Misc ===
LOG_LEVEL=INFO
